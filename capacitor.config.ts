import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inventory.resi',
  appName: 'Resi Inventory',
  webDir: '.output/public'
};

export default config;
